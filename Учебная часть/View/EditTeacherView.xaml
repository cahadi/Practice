<Page x:Class="Учебная_часть.View.EditTeacherView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Учебная_часть.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EditTeacherView">

    <Grid>
        <StackPanel VerticalAlignment="Top" Margin="0,28,0,0">
            <Label Content="Фамилия"/>
            <TextBox Text="{Binding EditTeacher.TeacherSurname}"/>
            <Label Content="Имя"/>
            <TextBox Text="{Binding EditTeacher.TeacherName}"/>
            <Label Content="Отчество"/>
            <TextBox Text="{Binding EditTeacher.TeacherPatronymic}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Вернуться" Command="{Binding BackToList}" Margin="70" Height="50" Width="100" FontSize="17"/>
                <Button Content="Сохранить" Command="{Binding SaveTea}" Margin="70" Height="50" Width="100" FontSize="17"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
