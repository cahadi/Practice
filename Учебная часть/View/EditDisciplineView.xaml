<Page x:Class="Учебная_часть.View.EditDisciplineView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Учебная_часть.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EditDisciplineView">
    <Grid>
        <StackPanel VerticalAlignment="Top" Margin="0,28,0,0">
            <Label Content="Индекс дисциплины"/>
            <TextBox Text="{Binding EditDiscipline.DisciplineIndex}"/>
            <Label Content="Название дисциплины"/>
            <TextBox Text="{Binding EditDiscipline.DisciplineName}"/>
            <Label Content="Тип дисциплины"/>
            <ComboBox ItemsSource="{Binding TypeDisciplines}" 
                      DisplayMemberPath="TypeDisciplines"
                      SelectedItem="{Binding EditDiscipline.TypeDisciplines}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Вернуться" Command="{Binding BackToList}" Margin="70" Height="50" Width="100" FontSize="17"/>
                <Button Content="Сохранить" Command="{Binding SaveDis}" Margin="70" Height="50" Width="100" FontSize="17"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
